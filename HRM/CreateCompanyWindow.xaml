<Window x:Class="HRM.CreateCompanyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HRM"
        mc:Ignorable="d"
        Title="CreateCompanyWindow" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" TextAlignment="Center" Grid.ColumnSpan="2">Создание компании</TextBlock>
        
        <TextBlock Grid.Row="1" Height="50" TextAlignment="Right" Margin="20">Название компании</TextBlock>
        <TextBox x:Name="companyName" Grid.Row="1" Grid.Column="1" Height="50"></TextBox>

        <Button x:Name="createCompanyButton" Grid.Row="2" Grid.ColumnSpan="2" Click="createCompanyButton_Click">Создать</Button>
    </Grid>
</Window>
