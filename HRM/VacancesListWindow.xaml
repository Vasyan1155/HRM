<Window x:Class="HRM.VacancesListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HRM"
        mc:Ignorable="d"
        Title="VacancesListWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid x:Name="vacanciesList" SelectionChanged="vacanciesList_SelectionChanged"></DataGrid>
        <Button x:Name="addButton" Grid.Row="1" Click="addButton_Click">Добавить</Button>
        <Button x:Name="removeButton" Grid.Row="2" Click="removeButton_Click">Удалить</Button>

    </Grid>
</Window>
